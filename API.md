# Pravah API

The canonical HTTP API and automation CLI reference lives in [`docs/api.md`](docs/api.md).

External agents should use the authenticated `pravah` CLI. Direct HTTP access is reserved for maintained integrations and administrative workflows.
