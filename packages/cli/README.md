# Pravah CLI

Authenticated JSON automation CLI for Pravah, a timeline-based task manager with AI agent API support.

This package is bun-targeted and requires bun at runtime:

```bash
bunx pravah --help
```

Inside the Pravah repository, use:

```bash
bun run pravah -- --help
```
